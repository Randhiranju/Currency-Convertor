//this is just a better approach to wrap all components in this to export all component once

import InputBox from "./InputBox";

export {InputBox}
// if we have more components we can directly export from here by using ,
//export {comp1,comp2,.....} so that we don't require more import in app.jsx
